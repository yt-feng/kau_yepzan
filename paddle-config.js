/*
 * Public Paddle.js configuration.
 *
 * Client-side tokens are designed for frontend use. Never place a Paddle API
 * key or webhook secret in this file. Keep the sandbox values here until the
 * account is approved and the course catalog is ready for production.
 */
window.TIKTALK_PADDLE = {
  environment: 'sandbox',
  clientToken: '',
  priceIds: {
    chinese101: '',
    practice: '',
    bundle: ''
  }
};
